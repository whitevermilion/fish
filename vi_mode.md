# Vi 模式命令

Vi 模式允许在提示符下使用类似 vi 的命令。最初，插入模式处于活动状态。按 `escape` 键进入命令模式。下面描述了在命令模式、插入模式和视觉模式下可用的命令。Vi 模式与 Emacs 模式共享一些按键绑定。

要启用 vi 模式，请使用 `fish_vi_key_bindings`。也可以通过以下方式将所有的 Emacs 模式绑定添加到 vi 模式：

```fish
function fish_user_key_bindings
    # 在每个应该使用 emacs 绑定的模式中执行一次
    fish_default_key_bindings -M insert

    # 然后执行 vi 绑定，以便在冲突时优先使用 vi 绑定。
    # 如果没有 --no-erase 参数，fish_vi_key_bindings 将默认重置所有绑定。
    # 参数指定初始模式（插入模式、"default" 或视觉模式）。
    fish_vi_key_bindings --no-erase insert
end
```

当处于 vi 模式时，`fish_mode_prompt` 函数将在提示符左侧显示模式指示器。要禁用此功能，请用一个空函数覆盖它。要在其他地方显示模式（如在右侧提示符中），请使用 `fish_default_mode_prompt` 函数的输出。

当绑定切换模式时，如果模式提示符存在，它将重绘模式提示符；如果模式提示符不存在，则重绘其余提示符。因此，如果你希望在你的 `fish_prompt` 中有一个模式指示器，你需要清除 `fish_mode_prompt`，例如通过在 `~/.config/fish/functions/fish_mode_prompt.fish` 中添加一个空文件。（改变模式的绑定应该调用重绘模式的绑定函数，参见 `bind`）

在支持的终端中，`fish_vi_cursor` 函数将用于根据模式更改光标形状。启用 vi 模式后，可以使用以下代码段手动配置光标：

```fish
# 模拟 vim 的光标形状行为

# 将普通模式和视觉模式的光标设置为方块
set fish_cursor_default block

# 将插入模式的光标设置为竖线
set fish_cursor_insert line

# 将替换模式的光标设置为下划线
set fish_cursor_replace_one underscore
set fish_cursor_replace underscore

# 将外部光标设置为竖线。外部光标在命令开始时出现。
# 当未指定 fish_cursor_external 时，光标形状采用 fish_cursor_default 的值。
set fish_cursor_external line

# 以下变量可用于配置视觉模式下的光标形状，
# 但由于 fish_cursor_default 的存在，这里显得多余
set fish_cursor_visual block
```

此外，可以在每个光标形状参数后添加 `blink` 来设置指定形状的闪烁光标。

Fish 支持 "block"（方块）、"line"（竖线）和 "underscore"（下划线）形状，其他值将被忽略。

如果设置上述变量后光标形状似乎没有改变，很可能你的终端模拟器不支持实现此功能所需的能力。

## 命令模式

命令模式也称为普通模式。

- `h`：光标向左移动。
- `l`：光标向右移动。
- `k` 和 `j`：在命令历史中搜索上一个/下一个包含在搜索开始前在命令行上指定的字符串的命令。如果搜索开始时命令行为空，则所有命令都匹配。有关历史搜索的更多信息，请参见历史部分。在多行命令中，它们分别向上和向下移动光标。
- `i`：在当前光标位置进入插入模式。
- `I`：在行首进入插入模式。
- `v`：在当前光标位置进入视觉模式。
- `a`：在当前光标位置之后进入插入模式。
- `A`：在行尾进入插入模式。
- `o`：在当前行下方插入新行并进入插入模式。
- `O`（大写字母 "O"）：在当前行上方插入新行并进入插入模式。
- `0`（零）：将光标移动到行首（保持在命令模式）。
- `d,d`：删除当前行并将其移动到复制粘贴（Kill Ring）。
- `D`：删除当前光标位置之后的文本并将其移动到复制粘贴（Kill Ring）。
- `p`：从复制粘贴（Kill Ring）粘贴文本。
- `u`：撤销命令行上最近的编辑。
- `ctrl-r`：重做最近的编辑。
- `[` 和 `]`：在命令历史中搜索上一个/下一个包含在搜索开始前光标下的标记的条目。有关历史搜索的更多信息，请参见历史部分。
- `/`：在分页器中打开历史记录。这将显示匹配搜索的历史条目，一次显示几个。再次按 `/` 将搜索更早的条目，按 `ctrl-s`（否则将切换分页器搜索）将转到较新的条目。搜索栏将始终被选中。
- `backspace`：光标向左移动。
- `g,g` / `G`：分别将光标移动到命令行的开头/结尾。
- `~`：切换字符的大小写（大写/小写）并移动到下一个字符。
- `g,u`：将到单词结尾的字符转换为小写。
- `g,U`：将到单词结尾的字符转换为大写。
- `:,q`：退出 fish。

## 插入模式

- `escape`：进入命令模式。
- `backspace`：向左删除一个字符。
- `ctrl-n`：接受自动建议。

## 视觉模式

- `left`（`←`）和 `right`（`→`）：将选择范围向后/向前扩展一个字符。
- `h`：光标向左移动。
- `l`：光标向右移动。
- `k`：光标向上移动。
- `j`：光标向下移动。
- `b` 和 `w`：将选择范围向后/向前扩展一个单词。
- `d` 和 `x`：将选择内容移动到复制粘贴（Kill Ring）并进入命令模式。
- `escape` 和 `ctrl-c`：进入命令模式。
- `c` 和 `s`：删除选择内容并切换到插入模式。
- `X`：将整行移动到复制粘贴（Kill Ring），并进入命令模式。
- `y`：将选择内容复制到复制粘贴（Kill Ring），并进入命令模式。
- `~`：切换选择内容的大小写（大写/小写），并进入命令模式。
- `g,u`：将选择内容转换为小写，并进入命令模式。
- `g,U`：将选择内容转换为大写，并进入命令模式。
- `"`, `*`, `y`：将选择内容复制到剪贴板，并进入命令模式。
